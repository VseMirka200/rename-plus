# Ренейм+ (Rename Plus)

Мощная утилита для переименования файлов с графическим интерфейсом и поддержкой командной строки.

## 🚀 Возможности

### Основные функции
- **Графический интерфейс** - удобный GUI на базе Tkinter
- **Командная строка** - полная поддержка CLI для автоматизации
- **Drag & Drop** - перетаскивание файлов в окно приложения
- **Предпросмотр** - просмотр результатов перед переименованием
- **История операций** - отслеживание всех переименований
- **Отмена/Повтор** - откат изменений при необходимости
- **Резервное копирование** - автоматическое создание бэкапов

### Методы переименования
- ✅ **Добавление/удаление текста** - префиксы и суффиксы
- ✅ **Замена текста** - простая замена подстрок
- ✅ **Регулярные выражения** - мощная замена через regex
- ✅ **Изменение регистра** - верхний, нижний, заглавный, title case
- ✅ **Нумерация** - автоматическая нумерация файлов
- ✅ **Метаданные** - переименование на основе метаданных файлов
- ✅ **Шаблоны** - использование шаблонов с переменными

### Поддерживаемые форматы

#### Изображения
`.jpg`, `.jpeg`, `.png`, `.gif`, `.bmp`, `.webp`, `.tiff`, `.tif`, `.ico`, `.svg`, `.heic`, `.heif`, `.avif`, `.dng`, `.cr2`, `.nef`, `.raw`

#### Документы
`.pdf`, `.docx`, `.doc`, `.xlsx`, `.xls`, `.pptx`, `.ppt`, `.txt`, `.rtf`, `.csv`, `.html`, `.htm`, `.odt`, `.ods`, `.odp`

#### Аудио
`.mp3`, `.wav`, `.flac`, `.aac`, `.ogg`, `.m4a`, `.wma`, `.opus`

#### Видео
`.mp4`, `.avi`, `.mkv`, `.mov`, `.wmv`, `.flv`, `.webm`, `.m4v`, `.mpg`, `.mpeg`, `.3gp`

### Дополнительные возможности
- 🔌 **Система плагинов** - расширяемость через плагины
- 🌍 **Интернационализация** - поддержка нескольких языков
- 🎨 **Темы оформления** - настраиваемый внешний вид
- 📊 **Статистика** - отслеживание использования
- 🔔 **Уведомления** - оповещения о завершении операций
- 🔄 **Проверка обновлений** - автоматическая проверка новых версий
- 🗑️ **Удаление метаданных** - очистка метаданных файлов
- 🔄 **Конвертация файлов** - конвертация между форматами

## 📋 Требования

- Python 3.7 или выше
- Tkinter (обычно входит в стандартную установку Python)

### Опциональные зависимости

Для полной функциональности рекомендуется установить:

```bash
pip install Pillow tkinterdnd2 pystray
```

- **Pillow** - работа с изображениями и метаданными
- **tkinterdnd2** - поддержка drag & drop
- **pystray** - системный трей

## 🛠️ Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/rename-plus.git
cd rename-plus
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

Или установите опциональные зависимости вручную:
```bash
pip install Pillow tkinterdnd2 pystray
```

## 🎯 Использование

### Графический интерфейс

Запустите приложение одним из способов:

```bash
python file_renamer.py
```

или

```bash
python Запуск.pyw
```

Файл `Запуск.pyw` запускает приложение без консольного окна.

### Командная строка

#### Базовое использование

```bash
# Добавить префикс
python cli.py --add-prefix "IMG_" *.jpg

# Заменить текст
python cli.py --replace "old" "new" *.txt

# Изменить регистр
python cli.py --case lower *.TXT

# Regex замена
python cli.py --regex "(\d+)" "№\1" *.txt

# Добавить нумерацию
python cli.py --number 1 *.jpg

# Предпросмотр без переименования
python cli.py --dry-run --case title *.txt

# Сохранить результаты в JSON
python cli.py --output results.json --case upper *.txt
```

#### Комбинирование методов

Методы применяются последовательно в порядке указания:

```bash
python cli.py --add-prefix "IMG_" --case title --number 1 *.jpg
```

#### Использование шаблонов

```bash
python cli.py --template "{date}_{name}" *.jpg
```

## 📁 Структура проекта

```
rename-plus/
├── core/                   # Основные модули
│   ├── backup_manager.py   # Управление резервными копиями
│   ├── file_converter.py   # Конвертация файлов
│   ├── file_operations.py  # Операции с файлами
│   ├── history_manager.py  # История операций
│   ├── metadata.py         # Извлечение метаданных
│   ├── metadata_remover.py # Удаление метаданных
│   ├── methods_manager.py  # Управление методами
│   ├── plugins.py          # Система плагинов
│   ├── rename_methods.py   # Методы переименования
│   └── script_engine.py   # Движок скриптов
├── managers/               # Менеджеры
│   ├── library_manager.py # Управление библиотеками
│   ├── settings_manager.py # Настройки
│   └── tray_manager.py     # Системный трей
├── ui/                     # Интерфейс
│   ├── drag_drop.py       # Drag & Drop
│   ├── theme_manager.py    # Темы
│   ├── ui_components.py    # Компоненты UI
│   └── window_utils.py     # Утилиты окон
├── utils/                  # Утилиты
│   ├── error_handler.py    # Обработка ошибок
│   ├── i18n.py            # Интернационализация
│   ├── logger.py          # Логирование
│   ├── notifications.py    # Уведомления
│   ├── statistics.py      # Статистика
│   └── update_checker.py  # Проверка обновлений
├── config/                 # Конфигурация
│   └── constants.py       # Константы
├── plugins/                # Плагины
├── materials/              # Ресурсы
│   └── icon/              # Иконки
├── file_renamer.py         # Главный файл GUI
├── cli.py                  # CLI интерфейс
└── Запуск.pyw             # Лаунчер
```

## 🔧 Разработка

### Запуск в режиме разработки

```bash
python file_renamer.py
```

### Тестирование CLI

```bash
python cli.py --help
```

## 📝 Лицензия

[Укажите вашу лицензию]

## 🤝 Вклад в проект

Приветствуются любые вклады! Пожалуйста:

1. Форкните проект
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📞 Контакты

[Укажите ваши контакты или ссылки на социальные сети]

## 🙏 Благодарности

- Разработчикам библиотек: Pillow, tkinterdnd2, pystray
- Всем контрибьюторам проекта

---

**Ренейм+** - делаем переименование файлов простым и удобным! 🚀

